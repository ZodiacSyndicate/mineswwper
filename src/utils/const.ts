export enum BlockType {
  Normal,
  Mine,
}

export enum BlockStatus {
  Unknown,
  Marked,
  Questionmarked,
  Known,
}

export enum GameStatus {
  Start,
  Win,
  Lose,
}

export const MouseButton = {
  Left: 1,
  Right: 2,
  Both: 3,
};
